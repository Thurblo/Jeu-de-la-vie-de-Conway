#include "Game.h"
#include "Cells.h"
#include "Grid.h"

int Game::getNbIteration(){
	return this->nb_iteration;
}

int Game::getNbIterationAsked() {
	return this->nb_iteration_asked;
}

void Game::iteration() {
	Grid grid;
	Cells& Grid::GetCells(int x,int y);
	while (nb_iteration > nb_iteration_asked)
	for (int x = 0; x < grid.GetWidth(); ++x) {
		for (int y = 0; y < grid.GetHeigt(); ++y) {
			if (grid.GetNeighbor())
	boucle sur liste de cell 
		si getNbCellEnVieAutour == 2 || 3
			cellule.envie = true,
		else
			cellule.envie = false;
			*/
}

